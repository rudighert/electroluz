$('.content_wrapper').html("<%= j @template.html_safe %>");
$('.content_wrapper').fadeIn(300);

if("<%= @name_render%>"=="init"){
  startScrollInit()
}

if("<%= @name_render%>"=="history"){
  actionForWho()
}

if("<%= @name_render%>"=="services"){
  servicesGalleries()
}

